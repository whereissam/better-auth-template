{
  "name": "better-auth-template",
  "version": "1.0.0",
  "description": "Better Auth + SIWE template with frontend and backend",
  "private": true,
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:build": "docker-compose up -d --build",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:restart": "docker-compose restart",
    "docker:clean": "docker-compose down -v",
    "db:only": "docker-compose up -d postgres",
    "dev:local": "concurrently \"cd backend && bun run dev\" \"cd frontend && bun run dev\"",
    "check:docker": "docker ps > /dev/null 2>&1 || (echo '❌ Docker is not running! Please start Docker Desktop first.' && exit 1)",
    "test": "concurrently \"cd frontend && bun run test:run\" \"cd backend && bun run test:run\"",
    "test:frontend": "cd frontend && bun run test:run",
    "test:backend": "cd backend && bun run test:run"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
